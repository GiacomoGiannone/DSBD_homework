# Alert Notifier - Deployment (Kafka consumer, sends emails)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-notifier
  namespace: dsbd
  labels:
    app: alert-notifier
spec:
  replicas: 1  # Single consumer for email sending
  selector:
    matchLabels:
      app: alert-notifier
  template:
    metadata:
      labels:
        app: alert-notifier
    spec:
      containers:
        - name: alert-notifier
          image: dsbd/alert-notifier:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: KAFKA_BROKER
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: kafka-broker
            - name: KAFKA_TOPIC_IN
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: kafka-topic-to-notifier
            - name: KAFKA_GROUP_ID
              value: "alert-notifier-group"
            - name: DISABLE_EMAIL
              valueFrom:
                secretKeyRef:
                  name: smtp-secrets
                  key: DISABLE_EMAIL
                  optional: true
            - name: SMTP_HOST
              valueFrom:
                secretKeyRef:
                  name: smtp-secrets
                  key: SMTP_HOST
            - name: SMTP_PORT
              valueFrom:
                secretKeyRef:
                  name: smtp-secrets
                  key: SMTP_PORT
            - name: SMTP_USER
              valueFrom:
                secretKeyRef:
                  name: smtp-secrets
                  key: SMTP_USER
            - name: SMTP_PASS
              valueFrom:
                secretKeyRef:
                  name: smtp-secrets
                  key: SMTP_PASS
            - name: SMTP_FROM
              valueFrom:
                secretKeyRef:
                  name: smtp-secrets
                  key: SMTP_FROM
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"

---
# Alert Notifier Service (optional, for health checks)
apiVersion: v1
kind: Service
metadata:
  name: alert-notifier
  namespace: dsbd
spec:
  selector:
    app: alert-notifier
  ports:
    - name: http
      port: 8000
      targetPort: 8000
